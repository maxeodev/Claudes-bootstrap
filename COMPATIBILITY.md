# Compatibilité / Compatibility

🇫🇷 Ce catalogue est testé et validé avec la version de Claude Code ci-dessous.
🇬🇧 This catalog is tested and validated against the Claude Code version below.

| | |
|---|---|
| **Claude Code (testé / tested)** | `2.1.x` (validé avec / validated with `2.1.186`) |
| **Validation** | `claude plugin validate --strict` (plugin + marketplace) |

## Notes

- 🇫🇷 Les plugins suivent le format documenté à [code.claude.com/docs](https://code.claude.com/docs/en/plugins-reference). En cas de changement de format dans une version ultérieure, ouvre une issue avec le label `compatibility`.
- 🇬🇧 Plugins follow the format documented at [code.claude.com/docs](https://code.claude.com/docs/en/plugins-reference). If a later version changes the format, open an issue with the `compatibility` label.
